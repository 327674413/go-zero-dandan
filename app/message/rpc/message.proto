syntax = "proto3";

// protoc-gen-go 版本大于1.4.0, proto文件需要加上go_package,否则无法生成
option go_package = "./pb";

package message;

message SendPhoneReq {
  string phone = 1;
  string phoneArea = 2;
  repeated string tempData = 3;
  int64 tempId = 4;
}
message SuccResp{
  //交易状态，默认200
  int64 code = 1;
  //成功返回的数据，用json来处理，暂未知是否需要这样
  string content = 2;
}


service message {
  rpc sendPhone(SendPhoneReq) returns(SuccResp);
}